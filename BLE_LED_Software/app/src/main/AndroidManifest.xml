<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ble_led">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/nene1"
        android:installLocation="auto"
        android:label="@string/app_name"
        android:roundIcon="@drawable/nene1"
        android:supportsRtl="true"
        android:theme="@style/FontStyle">
        <activity android:name=".LinkWaitingActivity" android:theme="@style/FontStyle"></activity>
        <activity android:name=".ColorPickActivity"
                  android:theme="@style/FontStyle">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    </application>

</manifest>